name: Integrate KDP print cover
on:
  push:
    branches: [main]
    paths:
      - scripts/integrate-kdp-print-cover.mjs
      - .github/workflows/integrate-kdp-print-cover.yml
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - run: node scripts/integrate-kdp-print-cover.mjs
      - name: Commit KDP bundle integration
        run: |
          if git diff --quiet; then
            echo "No source change produced" >&2
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add supabase/functions/enqueue-export-bundle/index.ts
          git commit -m "feat(kdp): ship full-wrap print cover in bundle"
          git pull --rebase origin main
          git push origin HEAD:main
