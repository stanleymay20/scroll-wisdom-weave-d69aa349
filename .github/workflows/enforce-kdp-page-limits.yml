name: Enforce KDP paperback page limits
on:
  push:
    branches: [main]
    paths:
      - scripts/enforce-kdp-page-limits.mjs
      - .github/workflows/enforce-kdp-page-limits.yml
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - run: node scripts/enforce-kdp-page-limits.mjs
      - name: Commit KDP page-limit enforcement
        run: |
          if git diff --quiet; then
            echo "No source change produced" >&2
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add supabase/functions/_shared/kdp-print-cover.ts
          git commit -m "fix(kdp): enforce current paperback page limits"
          git pull --rebase origin main
          git push origin HEAD:main
